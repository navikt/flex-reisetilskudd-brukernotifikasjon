logging.config: "classpath:logback-test.xml"


spring:
  profiles:
    active: test
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    properties:
      security.protocol: PLAINTEXT
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      group-id: "flex-reisetilskudd-brukernotifikasjon"
      max-poll-records: 1
      auto-offset-reset: earliest #TODO sett til none når offsett er etablert i prod
      enable-auto-commit: false
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver


on-prem-kafka:
  schema-registry.url: http://shchemagreier.nav
  bootstrap-servers: ${spring.embedded.kafka.brokers}
  security-protocol: plaintext

serviceuser:
  username: srvyo
  password: halaien

flex.reisetilskudd.frontend.url: http://www.nav.no/syk/reisetilskudd
